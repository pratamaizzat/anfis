export {}
/**
 * 2 inputs (x and y)
 *
 * rule 1: x = A1 and y = B1, then f1 = p1x + q1y + r1
 * rule 2: x = A2 and y = B2, then f2 = p2x + q2y + r2
 *
 * sugeno model-1: f = (w1xf1)+(w2xf2)/w1+w2
 *
 */
